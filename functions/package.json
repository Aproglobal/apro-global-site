{
  "name": "functions",
  "private": true,
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,hosting",
    "deploy": "npm run build && firebase deploy --only functions:lead",
    "lint": "eslint --ext .ts src || echo \"(eslint optional)\""
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.0",
    "firebase-functions": "^5.1.1"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
